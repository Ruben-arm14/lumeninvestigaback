package org.lumeninvestiga.backend.repositorio.tpi.utils;

//TODO: Revisar aplicación en las clases Entity.
public interface MethodList {
    <T> void add(T element);
    <T> void remove(T element);
}
